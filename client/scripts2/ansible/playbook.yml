---
- name: Configure machines basics
  hosts: all
  sudo: yes
  roles:
    - extra-repositories
    - iptables
    - selinux

- name: Install nginx web server
  hosts: all
  sudo: yes
  roles:
    - nginx